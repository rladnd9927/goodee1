<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://java.sun.com/xml/ns/javaee" xsi:schemaLocation="http://java.sun.com/xml/ns/javaee http://java.sun.com/xml/ns/javaee/web-app_3_0.xsd" id="WebApp_ID" version="3.0">
  <display-name>shop-3</display-name>
  <welcome-file-list>
    <welcome-file>index.html</welcome-file>
    <welcome-file>index.htm</welcome-file>
    <welcome-file>index.jsp</welcome-file>
    <welcome-file>default.html</welcome-file>
    <welcome-file>default.htm</welcome-file>
    <welcome-file>default.jsp</welcome-file>
  </welcome-file-list>
  
  <!-- Spring 설정하기 (서블렛설정하기 (shop-3-servlet.xml파일이 필요), 서블렛 매핑하기, 리스너 설정하기, 필터설정하기, 필터 매핑하기) -->
  <servlet>
	<servlet-name>shop-3</servlet-name><!-- 여기서 설정한 servlet-name에 '-servlet.xml'이 붙은 이름이 컨테이너에 적재되는 스프링 설정파일 기본이름임(WEB-INF폴더 밑에 존재해야함) -->
	<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
</servlet>  
<servlet-mapping>
	<servlet-name>shop-3</servlet-name>
	<url-pattern>*.shop</url-pattern><!-- ~~~~.shop 이라는 URL요청이 오면 해당 디스패쳐 서블렛을 사용하게 된다.  -> 이 디스패쳐 서블렛은 shop-3-servlet.xml설정파일을 사용하게 될것.-->
</servlet-mapping>


<!-- 
	ContextLoaderListener 의 기본설정파일은 /WEB-INF/applicationContext.xml파일이 디폴트 파일인데, 그것을 바꾸기 위해 <context-param>에서 설정한다.
	param-name은 contextConfigLocation, param-value에 그 경로와 이름을 입력.
	
	현재 아래 설정은 /src/common.xml 로 변경하는 예이다.
 -->
<context-param>
	<param-name>contextConfigLocation</param-name>
	<param-value>classpath:common.xml</param-value>
</context-param>

<listener>
	<listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
</listener>

<filter>
	<filter-name>CharacterEncodingFilter</filter-name>
	<filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
	<init-param>
		<param-name>forceEncoding</param-name>
		<param-value>true</param-value>
	</init-param>
	<init-param>
		<param-name>encoding</param-name>
		<param-value>euc-kr</param-value>
	</init-param>
</filter>

<filter-mapping>
	<filter-name>CharacterEncodingFilter</filter-name>
	<url-pattern>/*</url-pattern>
</filter-mapping>

<filter>
	<filter-name>sitemesh</filter-name>
	<filter-class>com.opensymphony.sitemesh.webapp.SiteMeshFilter</filter-class>
</filter>
<filter-mapping>
	<filter-name>sitemesh</filter-name>
	<url-pattern>*.shop</url-pattern>
</filter-mapping>
</web-app>