<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="dao.mapper.SemiUserMapper">
	<!-- 
	<select id="getSemiUser" parameterType="map" resultType="SemiUser">
		update semi_member set s_score=#{s_score}
	</select>
	 -->
</mapper>